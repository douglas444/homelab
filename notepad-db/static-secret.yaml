apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: vso-notepad-db-root-password
  namespace: notepad
spec:
  type: kv-v2
  mount: kv
  path: secret/kubernetes/notepad/db/root
  destination:
    name: notepad-db-root-password
    create: true
    type: Opaque
    transformation:
      excludeRaw: true
      excludes:
        - .*
      templates:
        "root-password":
          text: >-
            {{ .Secrets.password }}
  refreshAfter: 30s
  vaultAuthRef: static-auth
